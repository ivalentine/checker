<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-brand">
      <%= link_to "Taskdash &#10004;".html_safe, root_path %>
    </div>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
      <li role="presentation" class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.name %> <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li role="presentation"><%= link_to 'Edit profile', edit_user_registration_path %></li>
        </ul>
      </li>

      <li role="presentation"><%= link_to "Sign out", destroy_user_session_path, method: :delete, :class => 'navbar-link' %></li>
      <% else %>
      <li role="presentation"><%= link_to "Join", new_user_registration_path, :class => 'navbar-link' %></li>
      <li role="presentation"><%= link_to "Sign in", new_user_session_path, :class => 'navbar-link' %></li>
      <% end %>
    </ul>

  </div>
</nav>
