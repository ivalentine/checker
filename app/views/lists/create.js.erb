$("#new-list-container").replaceWith("<%= j render :partial => 'show' %>");

// $(".container").html("<%= j render 'show' %>");
// window.location.replace("<%= request.base_url %>/lists/<%= @list.id %>");
