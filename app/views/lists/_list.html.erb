<article class="col-sm-3 list-container">
  <h1><%= list.name %></h1>
  <ul>
    <% list.tasks.each do |task| %>
    <li><%= task.name %>
      <% if !task.due.nil? %>
      <small>due <%= task.due %></small>
      <% end %>
    </li>
    <% end %>
  </ul>
  <%= link_to 'Edit', edit_list_path(list) %>
</article>
